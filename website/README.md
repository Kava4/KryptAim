# AimSync website

Marketing site + docs (React + Vite + TypeScript).

## Develop

```bat
cd website
npm install
npm run dev
```

## Build

```bat
npm run build
```

Output: `dist/`

GitHub Pages: workflow **Deploy website** (Settings → Pages → GitHub Actions).
