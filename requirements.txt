# Rebuild — self-contained web UI + engines
makcu
Flask>=3.1
requests>=2.31
Werkzeug>=3.1

# NDI capture on KryptAim PC
numpy
opencv-python-headless
cyndilib

# YOLO trigger
ultralytics>=8.0
matplotlib>=3.7
onnxruntime>=1.18
# ONNX GPU: pip uninstall onnxruntime && pip install onnxruntime-gpu
